# Notebooks de référence

Le code exécutable est dans `src/coolpath/`. Les notebooks 22, 23, 24, 25, 29 et
30 fournis pour cette refonte sont listés dans `docs/SOURCES.json` avec leurs
empreintes SHA-256. Ils ne sont pas recopiés avec leurs sorties volumineuses.
Voir `MIGRATION.md` pour la correspondance entre notebooks et modules.
